# Solari GenAI Kit (self-hosted, controlled generation)

Starter kit to turn JSONL + domain tools into a **controlled** generative system.

## What you get
- `training/` : JSONL conversion + hygiene + Axolotl config templates (LoRA/QLoRA)
- `serve/`    : vLLM OpenAI-compatible server (Docker) + auth proxy skeleton
- `runtime/`  : schema-locked generation + tool router (TypeScript)
- `rag/`      : pgvector + ingest/retrieval contracts
- `evals/`    : golden tests + regression harness

Generated: 2026-02-26T00:38:50.394538Z
