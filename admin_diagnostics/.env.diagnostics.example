# DB
DATABASE_URL=postgresql://user:pass@localhost:5432/app?schema=public

# Redis
REDIS_URL=redis://localhost:6379

# Optional: gate who can access diagnostics without NextAuth (dev only)
DIAG_SHARED_SECRET=dev-only-secret

# Optional: webhook replay
STRIPE_WEBHOOK_SIGNING_SECRET=whsec_xxx
