apiVersion: v1
kind: Service
metadata:
  name: {{ include "business-spine.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "business-spine.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
