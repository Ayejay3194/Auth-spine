import type { ToolRegistry as ToolRegistryType } from "../types.js";

export const ToolRegistry: ToolRegistryType = {
  // Default empty tool registry
  // Tools can be registered dynamically at runtime
};

export default ToolRegistry;
